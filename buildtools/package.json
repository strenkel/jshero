{
  "name": "build-tools",
  "description": "Tools to build jshero (though it also runs locally)",
  "version": "1.0.0",
  "author": "jshero authors",
  "private": true,
  "scripts": {
    "comment_build": "run manually using $ ./node_modules/.bin/babel",
    "build": "babel -o ../js/koans-min/koans.js --minified ../js/koans/ && babel -o ../en/js/koans-min/koans.js --minified ../en/js/koans/"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "node-minify": "^2.4.0"
  }
}
