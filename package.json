{
  "name": "build-tools",
  "description": "Tools to build jshero (though it also runs locally)",
  "version": "1.1.0",
  "author": "jshero authors",
  "private": true,
  "scripts": {
    "build": "sh build/build.sh && node build/build.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/plugin-transform-template-literals": "^7.0.0",
    "ejs": "^2.6.1",
    "browserify": "^16.2.3"
  }
}
